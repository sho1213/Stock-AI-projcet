# Google Drive設定
# 共有ドライブを使う場合はドライブ名を指定。共有アイテムの場合は空にする
SHARED_DRIVE_NAME=
SOURCE_FOLDER_NAME=録画データ_all
TARGET_PARENT_FOLDER_NAME=チーム石川
TARGET_FOLDER_NAME=議事録

# Whisper設定
# モデルサイズ (large-v3 が最高精度。メモリ不足の場合は medium を使用)
WHISPER_MODEL_SIZE=large-v3
# デバイス (auto=GPU/CPU自動判定, cpu=CPU固定, cuda=GPU固定)
WHISPER_DEVICE=auto
# 計算精度 (CPU: int8 を推奨, GPU: float16 を推奨)
WHISPER_COMPUTE_TYPE=int8

# 実行制御
# 動画間の待機秒数
REQUEST_INTERVAL=5
# 1回の実行で処理する最大件数 (0=無制限)
MAX_VIDEOS_PER_RUN=50
